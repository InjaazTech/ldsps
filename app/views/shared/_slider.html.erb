<!-- Carousel
    ================================================== -->
<div id="myCarousel" class="carousel slide carousel-fade">
    <!-- Indicators -->
    <ol class="carousel-indicators">
        <% Slider.visible.each_with_index do |slider, idx|%>
        <li data-target="#myCarousel" data-slide-to="<%= idx %>" class="<%= 'active' if idx == 0%>"></li>
        <% end %>
    </ol>
    <div class="carousel-inner" role="listbox">
        <% Slider.visible.each_with_index do |slider, idx|%>
        <div class="item <%= 'active' if idx == 0%>">
        <img class="second-slide" src="<%= slider.image_url %>" alt="<%= slider.title(current_locale) %>">
        <% if slider.has_title?(current_locale)%>
            <div class="carousel-caption">
                <div class="text-box">
                    <h2 class="wow slideInRight" data-wow-duration="2s"><%= slider.title(current_locale) %></h2>
                    <p class="wow slideInLeft" data-wow-duration="2s"><%= slider.subtitle(current_locale) %></p>
                    <% if slider.has_link?%>
                        <p class="p-10 toggleCaption"><a class="btn" href="<%= slider.link %>" role="button"><%= slider.link_title(current_locale) %></a></p>
                    <% end %>
                </div>
            </div>
        <% end %>
        </div>
        <% end %>
    </div>
    <a class="left carousel-control" href="#myCarousel" role="button" data-slide="prev">
        <span class="glyphicon glyphicon-chevron-left" aria-hidden="true"></span>
        <span class="sr-only">Previous</span>
      </a>
      <a class="right carousel-control" href="#myCarousel" role="button" data-slide="next">
        <span class="glyphicon glyphicon-chevron-right" aria-hidden="true"></span>
        <span class="sr-only">Next</span>
      </a>
    </div><!-- /.carousel -->